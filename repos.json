{
  "_comment": "One entry per repo name (no org prefix). Group with comments for your own tracking; the sync script only reads the 'repos' array. Replace this example list with your own pilot repos.",
  "repos": [
    "alerts",
    "react-feature-template",
    "backend-api",
    "web-dashboard"
  ]
}
